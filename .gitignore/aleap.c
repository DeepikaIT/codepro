#incude<stdio.h>
int main()
{
int year;
printf("enter a year");
scanf("%d",&year);
if(year4%==0)
{
if(year100%==0)
{
if(year400%==0){
printf("%d is leap year",year);
else
printf("%dis not a leap year",year);
}
}
}
return 0;
}
